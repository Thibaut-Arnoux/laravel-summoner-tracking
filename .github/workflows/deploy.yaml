name: Deployment
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Get code
        uses: actions/checkout@v3
      # TODO : build docker images
